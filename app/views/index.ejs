<!DOCTYPE html>
<html lang="ja" ng-app="kimihate">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/bundle.css">
  <script src="/javascripts/bundle.js"></script>
</head>
<body ng-controller="globalController">
  <section class="global-header">
    <h1 class="title"><%= title %></h1>

    <a class="signin" ng-if="!user" href="/auth/signin">Twitterで登録/ログイン</a>

    <div class="user" ng-if="user" ng-click="go('me')">
      <img class="image" ng-src="{{user.thumbUrl}}">
      <span class="name">@{{user.name}}</span>
    </div>
  </section>

  <section class="global-subheader">
    <ul class="tabs">
      <li class="tab" ng-repeat="tab in tabs" ng-click="go('show', { id: tab.id })">{{tab.title}}</li>
      <li class="edit"><i class="fa fa-cog"></i></li>
    </ul>
  </section>

  <section class="global-content" ui-view></section>
</body>
</html>
